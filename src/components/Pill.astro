---
const { title, style } = Astro.props

const baseStyle = 'text-sm font-medium px-3 py-1 rounded-full'
const mainStyle = style ? `${baseStyle} ${style}` : baseStyle
---

<span class={mainStyle}>{title}</span>
